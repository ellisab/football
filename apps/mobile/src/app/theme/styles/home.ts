import type { AppTheme } from "../types";

export const createHomeStyles = (theme: AppTheme) => ({
  header: {
    paddingHorizontal: 12,
    paddingTop: 4,
    gap: 10,
  },
  heroCard: {
    marginHorizontal: 12,
    marginTop: 4,
    borderRadius: 36,
    borderWidth: 1,
    borderColor: theme.border,
    overflow: "hidden" as const,
    shadowColor: "#07030d",
    shadowOpacity: 0.5,
    shadowRadius: 32,
    shadowOffset: { width: 0, height: 18 },
    elevation: 10,
  },
  heroGradient: {
    overflow: "hidden" as const,
  },
  heroGlowWarm: {
    position: "absolute" as const,
    top: -48,
    left: -32,
    width: 190,
    height: 190,
    borderRadius: 95,
    backgroundColor: "rgba(255, 214, 108, 0.16)",
  },
  heroGlowCool: {
    position: "absolute" as const,
    top: 10,
    right: -22,
    width: 184,
    height: 184,
    borderRadius: 92,
    backgroundColor: "rgba(87, 235, 255, 0.15)",
  },
  heroGlowPink: {
    position: "absolute" as const,
    right: 34,
    bottom: -48,
    width: 172,
    height: 172,
    borderRadius: 86,
    backgroundColor: "rgba(255, 92, 154, 0.14)",
  },
  heroGrid: {
    paddingHorizontal: 20,
    paddingTop: 18,
    paddingBottom: 20,
    gap: 16,
  },
  heroKickerRow: {
    flexDirection: "row" as const,
    alignItems: "center" as const,
    gap: 8,
  },
  heroKickerDot: {
    width: 8,
    height: 8,
    borderRadius: 999,
    backgroundColor: theme.primary,
    shadowColor: theme.primary,
    shadowOpacity: 0.75,
    shadowRadius: 12,
    shadowOffset: { width: 0, height: 0 },
  },
  heroCopy: {
    gap: 10,
  },
  heroKicker: {
    color: theme.primary,
    fontSize: 11,
    fontFamily: theme.fonts.bodyBold,
    letterSpacing: 3,
    textTransform: "uppercase" as const,
    textShadowColor: "rgba(255, 214, 108, 0.32)",
    textShadowRadius: 18,
  },
  heroTitle: {
    color: theme.cream,
    fontSize: 38,
    lineHeight: 42,
    fontFamily: theme.fonts.heading,
    textTransform: "uppercase" as const,
    letterSpacing: 0.3,
  },
  heroDescription: {
    color: theme.textMuted,
    fontSize: 14,
    lineHeight: 20,
    fontFamily: theme.fonts.bodyMedium,
    maxWidth: 280,
  },
  heroBadges: {
    flexDirection: "row" as const,
    flexWrap: "wrap" as const,
    gap: 10,
  },
  heroBadge: {
    borderRadius: 999,
    borderWidth: 1,
    borderColor: theme.border,
    backgroundColor: "rgba(255,255,255,0.06)",
    paddingHorizontal: 12,
    paddingVertical: 7,
  },
  heroBadgeText: {
    color: theme.text,
    fontSize: 12,
    fontFamily: theme.fonts.bodySemibold,
  },
  section: {
    paddingHorizontal: 20,
    paddingTop: 2,
    marginTop: 14,
    gap: 6,
  },
  sectionKickerRow: {
    flexDirection: "row" as const,
    alignItems: "center" as const,
    gap: 8,
  },
  sectionKickerDot: {
    width: 8,
    height: 8,
    borderRadius: 999,
    backgroundColor: theme.primary,
    shadowColor: theme.primary,
    shadowOpacity: 0.7,
    shadowRadius: 12,
    shadowOffset: { width: 0, height: 0 },
  },
  sectionKicker: {
    color: theme.primary,
    fontSize: 11,
    fontFamily: theme.fonts.bodyBold,
    textTransform: "uppercase" as const,
    letterSpacing: 3,
    textShadowColor: "rgba(255, 214, 108, 0.24)",
    textShadowRadius: 12,
  },
  sectionTitle: {
    color: theme.cream,
    fontSize: 29,
    lineHeight: 31,
    fontFamily: theme.fonts.heading,
    textTransform: "uppercase" as const,
    letterSpacing: 0.2,
  },
  sectionSubtitle: {
    color: theme.textMuted,
    marginBottom: 8,
    fontSize: 14,
    lineHeight: 20,
    fontFamily: theme.fonts.bodyMedium,
  },
  tabs: {
    flexDirection: "row" as const,
    flexWrap: "wrap" as const,
    gap: 12,
    paddingHorizontal: 20,
    marginTop: -8,
  },
  tab: {
    paddingVertical: 10,
    paddingHorizontal: 15,
    borderRadius: 18,
    borderWidth: 1,
    borderColor: theme.border,
    backgroundColor: "rgba(255,255,255,0.05)",
    shadowColor: "#07030d",
    shadowOpacity: 0.2,
    shadowRadius: 14,
    shadowOffset: { width: 0, height: 8 },
    elevation: 4,
  },
  tabActive: {
    borderColor: theme.primary,
    shadowColor: theme.accentWarm,
    shadowOpacity: 0.22,
    shadowRadius: 12,
    shadowOffset: { width: 0, height: 4 },
    elevation: 5,
  },
  tabPressed: {
    opacity: 0.92,
  },
  tabText: {
    color: theme.textMuted,
    fontSize: 12,
    fontFamily: theme.fonts.bodyBold,
  },
  tabTextActive: {
    color: theme.cream,
  },
  quickActions: {
    marginHorizontal: 20,
    gap: 10,
  },
  quickActionCard: {
    minHeight: 110,
    borderRadius: 24,
    borderWidth: 1,
    borderColor: theme.border,
    backgroundColor: theme.surface,
    paddingHorizontal: 14,
    paddingVertical: 14,
    gap: 6,
    shadowColor: "#07030d",
    shadowOpacity: 0.18,
    shadowRadius: 16,
    shadowOffset: { width: 0, height: 8 },
    elevation: 5,
  },
  quickActionCardPressed: {
    borderColor: theme.accent,
    backgroundColor: theme.surfaceSoft,
  },
  quickActionCardDisabled: {
    opacity: 0.7,
  },
  quickActionLabel: {
    color: theme.primary,
    fontSize: 11,
    fontFamily: theme.fonts.bodyBold,
    letterSpacing: 2.2,
    textTransform: "uppercase" as const,
  },
  quickActionTitle: {
    color: theme.cream,
    fontSize: 21,
    lineHeight: 22,
    fontFamily: theme.fonts.heading,
    textTransform: "uppercase" as const,
    letterSpacing: 0.2,
  },
  quickActionDescription: {
    color: theme.textMuted,
    fontSize: 13,
    lineHeight: 18,
    fontFamily: theme.fonts.bodyMedium,
  },
  emptyState: {
    marginTop: 6,
    padding: 14,
    borderRadius: 20,
    borderWidth: 1,
    borderColor: theme.border,
    backgroundColor: theme.emptyBg,
  },
  emptyText: {
    color: theme.textMuted,
    textAlign: "center" as const,
    fontFamily: theme.fonts.bodyMedium,
  },
  errorBanner: {
    marginHorizontal: 20,
    marginTop: 4,
    padding: 14,
    borderRadius: 20,
    borderWidth: 1,
    borderColor: theme.warningBorder,
    backgroundColor: theme.warningBg,
  },
  errorText: {
    color: theme.warningText,
    fontSize: 13,
    lineHeight: 18,
    fontFamily: theme.fonts.bodyMedium,
  },
});
